package cmd

const (
	// windowsOS is the GOOS value for Windows
	windowsOS = "windows"

	// gitHubAPIHost is the default GitHub API host
	gitHubAPIHost = "github.com"

	// configDirPath is the XDG config directory path for gh-app-auth
	configDirPath = ".config/gh/extensions/gh-app-auth"
)
